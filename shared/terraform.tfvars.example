# Shared/hub configuration.
# vpc_cidr is optional (defaults to 172.31.0.0/24).

# Lock SSH to your admin IP/CIDR(s). SSM Session Manager also works, so you
# can set this to [] to disable SSH entirely and rely on SSM only.
ssh_ingress_cidrs = ["203.0.113.10/32"]
